<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %></title>
</head>
<body>
    <h1><%= post.title %> </h1>
    <p><%= post.description %> </p>
    <ul>
        <li><%= post.user.username %> </li>
        <li><%= post.createdAt %> </li>
    </ul>

    <a href="/<%= userId %>/posts/<%= post._id %>/edit"><button>Edit</button></a>
    <form action="/<%= userId %>/posts/<%= post._id %>?_method=DELETE" method="POST"><button>Delete</button></form>
    <a href="/<%= userId %>"><button>Back</button></a>

</body>
</html>